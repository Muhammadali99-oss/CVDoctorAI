import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'CVDoctor - AI-Powered CV Diagnosis & Role-Based Fixing',
  description: 'Transform your CV with AI-powered diagnosis and role-specific optimization for Central Asia, Russia, Kazakhstan, and China job markets.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}


